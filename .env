# Windows paths use backslashes, Unix paths use forward slashes
# Adjust paths according to your operating system
OBSIDIAN_VAULT_PATH=/home/jokh38/Documents/ObsidianVault

# Ollama API Server (Qwen3-Coder-30B model)
OLLAMA_BASE_URL=http://10.243.15.166:8080

CHROMA_DB_PATH=./chroma_db

# Local embedding model cache path (optional)
# If not set, will use default cache location
# EMBEDDING_MODEL_PATH=/home/user/.cache/llama_index/models--sentence-transformers--all-MiniLM-L6-v2/snapshots/...
API_HOST=0.0.0.0
API_PORT=8000
API_BASE_URL=http://localhost:8000

# Authentication Configuration
# Recommended: Use JWT tokens for production
# Get a JWT token by POST /token endpoint

# Development fallback simple token (only used in development mode)
# IMPORTANT: Replace with a strong, randomly generated token or disable for production
API_TOKEN=your_secure_token_here

# Environment settings
ENVIRONMENT=development

# JWT Configuration (primary authentication method)
# IMPORTANT: Replace with a strong, randomly generated secret key for production
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production

# JWT token expiration time in minutes (optional, defaults to 30)
JWT_EXPIRE_MINUTES=30


# Redis for Celery (if using background tasks)
REDIS_URL=redis://localhost:6379/0